# Stage 3: Frontend with Three.js & Cannon-es Setup

## ✅ Структура фронтенда создана

```
frontend/
├── src/
│   ├── App.vue              # Главный компонент
│   ├── main.js              # Точка входа
│   ├── style.css            # Глобальные стили с темами
│   ├── components/
│   │   └── Scene.vue        # Vue компонент для 3D сцены
│   ├── core/
│   │   └── DiceWorld.js     # Three.js + Cannon-es логика
│   └── stores/
│       └── game.js          # Pinia хранилище состояния
├── index.html               # HTML точка входа
├── package.json             # Зависимости
├── vite.config.js           # Конфиг сборщика
└── .gitignore              # Git исключения
```

## 🚀 Как запустить (Stage 3)

### Требования
- Node.js 16+ (установите с https://nodejs.org/)
- Работающий бэкенд (Stage 2, `docker-compose up --build`)

### Шаги запуска

1. **Откройте терминал и перейдите в папку фронтенда:**
   ```powershell
   cd frontend
   ```

2. **Установите зависимости:**
   ```powershell
   npm install
   ```

3. **Запустите dev-сервер:**
   ```powershell
   npm run dev
   ```

4. **Откройте браузер:**
   В терминале появится ссылка, обычно `http://localhost:5173`

## 📋 Что вы должны увидеть

### Визуальное
- **Фон**: Белый (clean) или черный (industrial)
- **3D сцена**: Освещенный виртуальный стол с тенью
- **UI**: Карточка управления внизу экрана

### Интерактивность
- **Кнопка "ROLL DICE"**: При клике падают кубики с физикой
- **Ползунок "COUNT"**: Меняет количество кубиков (1-10)
- **Кнопка темы**: Переключает между Clean 🔦 и Industrial ☢️ мерзостью

### Физика
- Кубики падают под гравитацией
- Удаются друг об друга
- Катаются по столу с затуханием (friction)
- Отскакивают при падении (restitution)

## 🎨 Темы

### Clean Mode (по умолчанию)
- Фон: белый
- Кубики: зеленые (#34d399)
- Свет: мягкий дневной
- Шрифт: Inter (гарнитура)

### Industrial Mode
- Фон: черный
- Кубики: ржаво-коричневые (#78350f)
- Свет: оранжевый "прожектор" (#ea580c)
- Шрифт: VT323 (монопространство)
- Эффект CRT: линии + шум (из CSS)

## 🛠️ Структура кода

### DiceWorld.js
**Ядро 3D и физики**
- `constructor(canvas)` — инициализирует Three.js и Cannon.js
- `createFloor()` — пол со физикой
- `createLights()` — система освещения
- `addDice()` — добавляет один кубик D6
- `throwDice(count)` — основной метод броска
- `setTheme(name)` — переключение темы
- `animate()` — главный цикл рендеринга

### Scene.vue
**Vue компонент сцены**
- Инициализирует `DiceWorld` в `onMounted()`
- Слушает смену темы через `watch`
- Предоставляет метод `triggerThrow()` для родителя

### game.js (Pinia store)
**Глобальное состояние**
- `diceType` — тип кубика (D6)
- `diceCount` — количество кубиков
- `currentTheme` — текущая тема
- `toggleTheme()` — переключатель тем

### App.vue
**Главный компонент**
- Размещает Scene на фоне
- UI overlay с кнопками сверху и панелью внизу
- Обработчик клика на "ROLL DICE"

## ⚙️ Конфигурация

### vite.config.js
- Порт dev-сервера: **5173**
- Проксирование API: `/api` → `http://localhost:8080`

### CORS
Backend должен разрешать запросы с `http://localhost:5173` (смотри `backend/cmd/server/main.go`)

## 🧪 Тестирование

**Test 1: Броски работают?**
```
1. Нажмите "ROLL DICE"
2. Должны упасть кубики и покатиться
```

**Test 2: Темы работают?**
```
1. Нажмите кнопку "🔦 MODE"
2. Фон изменится, кубики переокрасятся, свет изменится
```

**Test 3: Количество кубиков?**
```
1. Переместите ползунок COUNT до 5
2. Нажмите "ROLL DICE"
3. Должно упасть 5 кубиков одновременно
```

## 📦 Сборка продакшена

Для создания оптимизированной версии:
```powershell
npm run build
```

Результат появится в папке `dist/`. Его можно разместить на статическом хостинге.

## 🔗 Связь с бэкендом (Stage 4)

На данный момент фронтенд полностью локален. В Stage 4 мы добавим:
- WebSocket соединение
- Синхронизацию бросков между игроками
- Сохранение результатов на сервер
